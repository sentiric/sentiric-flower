FROM mher/flower:latest

# (Opsiyonel) Tema, logo, vs. eklemek istersen buraya koyabilirsin
# COPY ./custom-theme /app/custom-theme

# Örneğin oturum koruması, reverse proxy uyumu vs.
# ENV FLOWER_BASIC_AUTH=admin:secret

# Komut aynı kalabilir veya override edebilirsin
# CMD ["flower", "--port=5555"]
